# # ==============================================================================
# # GitHub Actions Workflow: LSMS-ISA Harmonised Panel Analysis
# #
# # Jobs:
# #   1. syntax-check – parse() all R scripts
# #   2. full-pipeline – install packages, run all scripts sequentially
# #
# # Triggers:
# #   - Push to main/develop branches
# #   - Changes to R_scripts/, R_data/, or .github/workflows/
# #   - Pull requests to main
# #   - Manual workflow_dispatch
# # ==============================================================================
# 
# name: LSMS-ISA Pipeline
# 
# on:
#   push:
#     branches: [main, develop]
#     paths:
#       - 'R_scripts/**'
#       - 'R_data/**'
#       - '.github/workflows/**'
#   pull_request:
#     branches: [main]
#     paths:
#       - 'R_scripts/**'
#       - 'R_data/**'
#       - '.github/workflows/**'
#   workflow_dispatch:
# 
# jobs:
#   # --------------------------------------------------------------------------
#   # JOB 1: Syntax Check
#   # --------------------------------------------------------------------------
#   syntax-check:
#     runs-on: ubuntu-22.04
#     timeout-minutes: 10
#     steps:
#       - uses: actions/checkout@v4
# 
#       - uses: r-lib/actions/setup-r@v2
#         with:
#           r-version: '4.3.3'
#           use-public-rspm: true
# 
#       - name: Install R packages for syntax check
#         run: |
#           Rscript -e '
#           install.packages(c("here", "stringr"), dependencies = TRUE)
#           '
# 
#       - name: Parse R scripts
#         run: |
#           find R_scripts -name "*.R" -type f -print0 | while IFS= read -r -d '' file; do
#             echo "Checking syntax: $file"
#             Rscript -e "parse(file = '$file')"
#             if [ $? -ne 0 ]; then
#               echo "Syntax error in $file"
#               exit 1
#             fi
#           done
# 
#   # --------------------------------------------------------------------------
#   # JOB 2: Full Pipeline
#   # --------------------------------------------------------------------------
#   full-pipeline:
#     runs-on: ubuntu-22.04
#     needs: syntax-check
#     permissions:
#       contents: write
#     timeout-minutes: 120
# 
#     steps:
#       - uses: actions/checkout@v4
# 
#       # --- INSTALL SYSTEM DEPENDENCIES ---
#       - name: Install system dependencies
#         run: |
#           sudo apt-get update -qq
#           sudo apt-get install -y --no-install-recommends \
#             libglpk-dev \
#             libgdal-dev \
#             libgeos-dev \
#             libproj-dev \
#             libudunits2-dev \
#             libmagick++-dev \
#             libcurl4-openssl-dev \
#             libssl-dev \
#             libxml2-dev \
#             pandoc \
#             libharfbuzz-dev \
#             libfribidi-dev \
#             libfreetype6-dev \
#             libpng-dev \
#             libtiff5-dev \
#             libjpeg-dev
#           sudo ldconfig
# 
#       - uses: r-lib/actions/setup-r@v2
#         with:
#           r-version: '4.3.3'
#           use-public-rspm: true
# 
#       - name: Install R packages
#         run: |
#           Rscript -e '
#           # Core packages used across all scripts
#           install.packages(c(
#             "tidyverse",
#             "haven",
#             "labelled",
#             "stringr",
#             "purrr",
#             "data.table",
#             "lubridate",
#             "mice",
#             "psych",
#             "stats",
#             "here"
#           ), dependencies = TRUE)
#           
#           # Additional packages for analysis and reporting
#           install.packages(c(
#             "broom",
#             "car",
#             "corrplot",
#             "factoextra",
#             "FactoMineR",
#             "ggplot2",
#             "gridExtra",
#             "knitr",
#             "markdown",
#             "rmarkdown",
#             "scales",
#             "stargazer",
#             "xtable",
#             "forcats"
#           ), dependencies = TRUE)
#           '
# 
#       # --- RUN MASTER SCRIPT ---
#       - name: Run MASTER.R
#         run: |
#           cd R_scripts
#           Rscript --vanilla MASTER.R
# 
#       # --- UPLOAD OUTPUTS ---
#       - name: Upload outputs
#         uses: actions/upload-artifact@v4
#         if: always()
#         with:
#           name: outputs
#           path: |
#             R_data/Temp/
#             R_data/Final/
#             Output/
#           retention-days: 14
# 
#       # --- COMMIT OUTPUTS BACK TO REPO ---
#       - name: Commit outputs to repo
#         if: always() && github.ref == 'refs/heads/main'
#         run: |
#           git config user.email "ci-bot@lsms-isa-panel.dev"
#           git config user.name "CI Bot"
#           git add -A R_data/Temp/ R_data/Final/ Output/ || true
#           git diff --cached --quiet || git commit -m "ci: update outputs [skip ci]"
#           git push origin ${{ github.ref_name }} --force-with-lease || true
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
# 
#   # --------------------------------------------------------------------------
#   # JOB 3: Validation Summary (optional)
#   # --------------------------------------------------------------------------
#   validation-summary:
#     runs-on: ubuntu-22.04
#     needs: full-pipeline
#     if: always() && github.ref == 'refs/heads/main'
#     timeout-minutes: 15
# 
#     steps:
#       - uses: actions/checkout@v4
# 
#       - uses: r-lib/actions/setup-r@v2
#         with:
#           r-version: '4.3.3'
#           use-public-rspm: true
# 
#       - name: Install R packages for validation
#         run: |
#           Rscript -e '
#           install.packages(c("tidyverse", "haven", "here"), dependencies = TRUE)
#           '
# 
#       - name: Download outputs
#         uses: actions/download-artifact@v4
#         with:
#           name: outputs
#           path: ./artifacts
# 
#       - name: Generate validation report
#         run: |
#           Rscript -e '
#           library(tidyverse)
#           library(haven)
#           
#           # Check if outputs exist
#           output_dir <- "./artifacts"
#           if (dir.exists(output_dir)) {
#             final_dir <- file.path(output_dir, "R_data/Final")
#             if (dir.exists(final_dir)) {
#               files <- list.files(final_dir, pattern = "\\.dta$", full.names = TRUE)
#               if (length(files) > 0) {
#                 cat("## Validation Report\n\n")
#                 cat("### Files Generated:\n\n")
#                 cat("| File | Rows | Columns | Households | Plots | Crops |\n")
#                 cat("|------|------|---------|------------|-------|-------|\n")
#                 for (f in files) {
#                   tryCatch({
#                     df <- read_dta(f)
#                     n_rows <- nrow(df)
#                     n_cols <- ncol(df)
#                     n_hh <- if ("hhid" %in% names(df)) length(unique(df$hhid)) else NA
#                     n_plots <- if ("plot_id" %in% names(df)) length(unique(df$plot_id)) else NA
#                     n_crops <- if ("cropcode" %in% names(df)) length(unique(df$cropcode)) else NA
#                     cat(sprintf("| %s | %d | %d | %s | %s | %s |\n", 
#                                 basename(f), n_rows, n_cols, 
#                                 ifelse(is.na(n_hh), "-", n_hh),
#                                 ifelse(is.na(n_plots), "-", n_plots),
#                                 ifelse(is.na(n_crops), "-", n_crops)))
#                   }, error = function(e) {
#                     cat(sprintf("| %s | Error | - | - | - | - |\n", basename(f)))
#                   })
#                 }
#                 cat("\n")
#                 cat("### Summary Statistics:\n\n")
#                 
#                 # Look for household files
#                 hh_files <- files[grepl("hh", basename(files), ignore.case = TRUE)]
#                 if (length(hh_files) > 0) {
#                   cat("#### Household-level files found:\n")
#                   for (f in hh_files) {
#                     cat(sprintf("- %s\n", basename(f)))
#                   }
#                 }
#                 
#                 plot_files <- files[grepl("plot", basename(files), ignore.case = TRUE)]
#                 if (length(plot_files) > 0) {
#                   cat("\n#### Plot-level files found:\n")
#                   for (f in plot_files) {
#                     cat(sprintf("- %s\n", basename(f)))
#                   }
#                 }
#                 
#                 crop_files <- files[grepl("crop", basename(files), ignore.case = TRUE)]
#                 if (length(crop_files) > 0) {
#                   cat("\n#### Crop-level files found:\n")
#                   for (f in crop_files) {
#                     cat(sprintf("- %s\n", basename(f)))
#                   }
#                 }
#                 
#                 indiv_files <- files[grepl("indiv", basename(files), ignore.case = TRUE)]
#                 if (length(indiv_files) > 0) {
#                   cat("\n#### Individual-level files found:\n")
#                   for (f in indiv_files) {
#                     cat(sprintf("- %s\n", basename(f)))
#                   }
#                 }
#                 
#               } else {
#                 cat("No .dta files found in output directory.\n")
#               }
#             } else {
#               cat("Final directory not found.\n")
#             }
#           } else {
#             cat("No artifacts downloaded.\n")
#           }
#           ' > validation_report.md
# 
#       - name: Upload validation report
#         uses: actions/upload-artifact@v4
#         with:
#           name: validation-report
#           path: validation_report.md
#           retention-days: 30
# 
# # ==============================================================================
# # END OF WORKFLOW
# # ==============================================================================